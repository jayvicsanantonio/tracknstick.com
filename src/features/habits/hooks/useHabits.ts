// Re-export the offline-first implementation for backwards compatibility
// This maintains the same interface while providing offline-first functionality
export { useHabitsOffline as useHabits } from './useHabitsOffline';
